# 이 레포는 공개 npm 레지스트리만 사용한다(사내 nexus 미사용).
# 개인 ~/.npmrc 가 사내 nexus 를 가리켜도 프로젝트 .npmrc 가 우선하므로,
# 어느 머신/CI 에서 install 해도 의존성이 nexus 를 타지 않는다.
# (참고: lockfile resolved URL 의 nexus→공개 전환은 커밋 d49f43d 에서 수행)
registry=https://registry.npmjs.org/
